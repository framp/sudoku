{
  "name": "sudoku",
  "scripts": {
    "build": "cp ./src/ports/solver.js ./build/solver.js && elm-make ./src/App.elm --output=./build/sudoku.js"
  },
  "devDependencies": {
    "elm": "^0.18.0"
  }
}
