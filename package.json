{
  "name": "sudoku",
  "scripts": {
    "build": "elm-make ./src/App.elm --output=./build/elm.js && cat build/elm.js src/ports/solver.js > build/sudoku.js && rm build/elm.js"
  },
  "devDependencies": {
    "elm": "^0.18.0"
  }
}
